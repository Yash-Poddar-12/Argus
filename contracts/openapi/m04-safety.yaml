# Skeleton owned by M04. Replace in M04-WP0 (hand-written contract first, then generated from code
# with `uv run python scripts/contracts.py export` once the module has a router).
openapi: 3.1.0
info:
  title: M04 IoT Hazard Mesh / Safety API
  version: 0.0.0
  description: PLANNED. See docs/02-contracts/API_CATALOG.md section M04.
paths: {}
